[project]
name = "lyseya"
version = "0.1.0"
description = "Local first chat box"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx[http2]>=0.28.1",
    "prompt-toolkit>=3.0.51",
]
